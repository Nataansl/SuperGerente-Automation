{
  "name": "qa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open:chrome": "npx cypress open --browser chrome",
    "cy:run:chrome": "npx cypress run --browser chrome",
    "cy:run:report": "cypress run --reporter cypress-mochawesome-reporter",
    "cypress:open:dev": "cross-env ENV=dev cypress open --browser chrome",
    "cypress:open:prod": "cross-env ENV=prod cypress open --browser chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^10.1.0",
    "cypress": "^15.12.0",
    "cypress-mochawesome-reporter": "^4.0.2",
    "cypress-multi-reporters": "^2.0.5",
    "dotenv": "^17.3.1",
    "mocha-junit-reporter": "^2.2.1"
  },
  "dependencies": {
    "dayjs": "^1.11.20"
  }
}
